/** PKR amount a creator's billed-and-charged balance must reach before a payout is requestable
 *  (Playbook 05 E7). Unconfirmed real number — same class of flagged placeholder as
 *  lib/merchant/constants.ts's PLATFORM_FEE_RATE, swap once the real threshold is known. */
export const PAYOUT_THRESHOLD_PKR = 5000;
